import 'package:flutter/material.dart';

class ClubeBotiPage extends StatelessWidget {
  const ClubeBotiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Clube Boti'),
        automaticallyImplyLeading: true,
      ),
      body: Container(),
    );
  }
}
